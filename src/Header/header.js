import { Button } from '../Button/Button';

export const Header = () => {
    return (
        <div>
            <Button>Paspausk mane</Button>
            <Button>Register</Button>
        </div>
    )
}